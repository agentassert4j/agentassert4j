# <域>规格

> 最近复核：<commit 短哈希> / <yyyy-MM-dd> · <复核方式，如「会话内全量代码对账」>
> 验证三档占比：【测试钉】N 条 ·【命令可证】N 条 ·【人工对账】N 条（人工对账占比越低，规格越健康）

<!-- 复制本模板成文时：删除全部 HTML 注释与占位符；每条契约/行为声明必须带验证档位标注。 -->

## 职责与边界

<!-- 本域管什么、不管什么，控制在一屏内。边界写成「不管什么」的排除式清单更有约束力。 -->

## 真源与派生

<!-- 每项语义状态指明：唯一真源是什么、由谁携带、派生链与就绪顺序。
     派生表示（键/哈希/汇总/报告形态）只作投影消费；真源缺席的重建路径必须钉在可测不变式上。 -->

## 状态机与生命周期

<!-- mermaid 状态图 + 事件×前置条件×动作×终态转移表。没有状态机的域写「本域无状态机」并说明原因。 -->

## 契约

<!-- 对外承诺逐条列出，每条带验证档位标注：
     - 【测试钉】：指名测试类——代码变更破坏本条描述时测试变红（自动尖叫）。
     - 【命令可证】：指名命令与可观察输出（如 status --diff 的某行、exit code）。
     - 【人工对账】：纯叙述、暂无机械验证——最危险档位，复核台账跟踪其收缩。 -->

## 行为矩阵

<!-- 输入×前置条件→结果。错误路径与退化行为必须成行，不允许只写 happy path。 -->

## 域间边界

<!-- 上游：本域消费谁的什么产出（含消费前提与就近校验）。
     下游：本域向谁供给什么（含格式契约）。 -->

## 变更纪律

<!-- 哪些是冻结契约（改动走什么流程/是否单向门）、哪些是自由演进面。
     冻结语义：判定语义/指纹算法定义、存储 schema 列、公开 SPI 签名——变更属单向门，
     须在变更描述中显式标注影响与迁移方案。 -->

## 复核台账

<!-- 每次对账追加一行：日期 / 方式 / 发现（含成文时发现的文档-vs-代码失真点及其处置）。 -->
